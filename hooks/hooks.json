[
  {
    "type": "PostToolUse",
    "matcher": {
      "tool_name": "^(Write|Edit)$"
    },
    "hooks": [
      {
        "type": "command",
        "command": "file=\"$(echo '$TOOL_INPUT' | jq -r '.file_path // empty')\" && [ -n \"$file\" ] && echo \"$file\" | grep -q 'SKILL\\.md$' && command -v aigent >/dev/null 2>&1 && aigent validate \"$(dirname \"$file\")\" 2>&1 || true"
      }
    ]
  }
]
